version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  model-inference:
    lang: dockerfile
    handler: ./model-inference
    image: nathan8an/model-inference:latest

